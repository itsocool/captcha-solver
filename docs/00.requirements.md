# 요구사항 정의서

이 문서는 2026-09-23 `/data-source` 사용자 피드백으로 확정된 범위를 기록한다. 기존 앱 전체의 요구사항이나 완료 상태를 소급하여 확정하지 않는다.

| ID | 확정 요구사항·수용 기준 | 구현 근거 |
|---|---|---|
| DS-001 | 신뢰도 기준 2개를 입력한다. 기본값은 첫 번째 0.92, 두 번째 0.86. 0 ≤ 두 번째 ≤ 첫 번째 ≤ 1이며 빈 값·범위 초과·역전은 예측 실행 전에 거부한다. 첫 번째 이상~1은 녹색, 두 번째 이상~첫 번째 미만은 주황색, 두 번째 미만은 빨간색이다. 기준 변경 시 기존 카드 색상을 즉시 갱신한다. 기존 자동 이름 변경 하한은 두 번째 기준을 사용한다. | `apps/web/templates/data_source.html`, `apps/web/static/js/data_source.js` |
| DS-002 | 카드의 라벨 입력과 신뢰도에 같은 구간 색상을 적용하고 신뢰도는 소수점 두 자리로 표시한다. 신뢰도는 카드 하단의 별도 행에 고정 표시한다. 색상 판정과 정렬에는 반올림 전 원본 값을 사용한다. 예측 기록이 없거나 유효하지 않으면 중립색 `신뢰도 —`로 표시한다. | SQLite `data_source_predictions`의 대상·리비전·파일별 신뢰도를 `/drafts` 응답으로 조회한다. 새 탭·재접속에서도 복원하며 수동 라벨 수정 시 이미지의 모델 예측 신뢰도를 새 파일명으로 연결한다. |
| DS-003 | 이미지 정렬은 신뢰도 내림차순, 동률이면 생성 시점 오름차순이다. 미예측 이미지는 마지막에 생성 순으로 배치한다. 예측 이벤트·갤러리 새로 고침 시 적용하며 라벨 편집 중에는 이동을 보류한다. | 현행 `list_drafts()`의 파일 mtime 오름차순을 생성 순서로 재사용한다. rename은 mtime을 유지한다. |
| DS-004 | 예측값과 신뢰도를 SQLite에 영속 저장한다. 자동 라벨링에서 하한 미달·이름 충돌인 이미지도 성공한 예측은 보존한다. 기존 이미지의 누락 신뢰도는 “신뢰도 계산”으로 재추론하되 파일명·내용을 변경하지 않는다. 선택한 리비전의 모델을 사용하며 이미 유효한 저장값이 있으면 다시 추론하지 않는다. 이미지 크기·mtime이 달라지면 과거 값을 표시하지 않는다. 저장 실패를 숨기지 않고 이름 변경 전 차단하며, 이름 변경 커밋 실패 시 파일명을 복원한다. | `db/schema.sql` migration 12, `apps/web/core/db.py`, `services/data_source.py`, `/data-source/confidence/stream` |
| DS-005 | 라벨 입력에서 포커스가 벗어나면(클릭·Tab·Enter, 마지막 카드 포함) 저장하며 입력한 레이블값으로 실제 이미지 파일명도 `<레이블>.png`로 변경한다. 성공 응답 후 카드의 파일명·이미지 URL을 갱신하고 새로고침 후에도 유지한다. 이름 충돌·유효하지 않은 파일명·저장 실패 시 기존 파일을 보존하고 오류를 표시한다. 값이 그대로여도 비어 있지 않은 라벨을 확인한 것으로 저장하며, 성공하면 확인/편집 전·후 파일명과 저장 시각을 DB 이력으로 기록하고 하단 신뢰도 줄 왼쪽에 체크 배지를 표시한다. 새로고침·재접속·추가 편집 후에도 배지를 복원한다. 자동 이름 변경·실패·빈 값은 수동 이력을 추가하지 않는다. 저장 중 중복 제출을 방지하고 저장 성공 후 체크를 표시한다. 이미지 교체 시 이전 배지를 표시하지 않는다. DB 저장과 파일 변경은 같은 성공/실패 단위로 처리한다. | `data_source_label_edits`, migration 13, `/data-source/label`, `/data-source/drafts` |
| DS-006 | 레이블 글자 크기는 이미지가 실제 표시되는 크기(object-contain 비율)와 레이블 길이에 따라 12~36px 범위에서 조절한다. 이미지 비례 계수는 0.62로 적용하고 긴 레이블은 입력 폭에 맞춰 축소한다. 이미지 로딩·화면 크기 변경·입력 편집·저장 후 갱신하며, 이미지 정보가 없으면 기존 22px를 사용한다. | `calculateLabelFontSize`, `ResizeObserver`, `resizeLabel` |
| DS-007 | “체크된 이미지 train으로 이동” 버튼으로 선택한 캡차·리비전의 DB 수동 확인 기록이 현재 이미지 크기·mtime과 일치하는 draft PNG만 같은 리비전 train 폴더로 이동한다. 진행 중인 라벨 저장을 기다리고 이동 중 중복 실행·대상 변경·편집을 막는다. 파일 내용·mtime·DB 이력을 보존하며 기존 파일은 덮어쓰지 않는다. 충돌·실패 파일은 draft에 남기고 이동·충돌·실패 개수를 표시한다. 완료·오류 후 목록을 재조회한다. 수집·예측 중 이동 요청은 거부한다. | `move_checked_to_train`, `/data-source/move-to-train`, `moveCheckedToTrain` |
| DEV-001 | FastAPI 개발 서버는 별도 포트 옵션이 없을 때 기본 포트 5000에서 시작한다. 루트 실행 문서와 개발 실행 예제도 `http://localhost:5000`을 사용한다. | `apps/web/core/config.py`의 `Settings.web_port`, `apps/web/__main__.py`, 루트 `README.md` |

## 현행 기준과 제한

- 기존에 이름이 붙은 파일은 자동 이름 변경에서 제외되지만 “신뢰도 계산”에서는 누락 예측의 대상이다.
- 신뢰도는 저장된 모델 예측에 대한 값이며 수동 입력한 정답의 정확도를 의미하지 않는다. 모델 재학습 후 기존 저장값을 일괄 갱신하는 기능은 이번 범위에 포함하지 않는다.
- 수동 확인·편집 이력은 DS-005 적용 후 성공한 저장부터 기록한다. 기존 파일명을 편집 이력으로 추정하지 않는다.
- 현행 생성 순서는 파일 mtime 기준이며 파일시스템 birth time을 사용하지 않는다.

## 검증

| 대상 | 명령·수용 기준 |
|---|---|
| DS-001~007 | `apps/web/.venv/bin/python -m pytest tests/test_data_source_predictions.py tests/test_data_source_confidence.py tests/test_web_project.py tests/test_context_path.py -q`: 경계값·미예측·입력 오류·대상 분리·정렬·편집 보존·기존 경로 회귀. 프런트엔드 회귀에는 Node.js가 필요하다. |
| JS 문법 | `node --check apps/web/static/js/data_source.js` |
| 화면 | 개발 URL에서 기본값, 실제 CSS 색상, SSE/완료 후 새로 고침, 기준 수정, 정렬과 데스크톱·모바일 오버플로를 확인한다. 테스트용 SSE로 검사할 때 실제 모델 추론·파일명 변경 검증과 구분한다. |

2026-09-23 검증 결과(하단 행 변경 전): 대상 pytest 12건과 JS 문법 검사 통과. 개발 URL에서 Orca로 기본값·플로팅 위치를 확인했고, 별도 Chromium 테스트용 SSE로 경계색·소수점 두 자리·입력 거부·실시간 갱신·세션 복원·정렬·플로팅의 높이 미점유를 검증했다. 1194×646, 937×642, 390×844에서 가로 넘침이 없었고 페이지 JS 오류가 없었다. 실제 모델 추론과 사용자 파일 이름 변경은 실행하지 않았다. 기존 Starlette 관련 deprecation 경고 2건은 남아 있다.

2026-09-23 후속 변경 검증: 신뢰도 표시를 카드 하단 고정 행으로 변경하고 `node --check apps/web/static/js/data_source.js` 및 `apps/web/.venv/bin/python -m pytest tests/test_data_source_confidence.py -q`를 통과했다. 하단 행의 실제 브라우저 배치와 반응형 오버플로는 주 에이전트의 화면 검증 대상이다.

하단 고정 행 브라우저 검증: 개발 URL 937×646에서 신뢰도 행의 `position: static`, 라벨 입력 바로 아래 배치(입력 하단과 신뢰도 행 상단 일치), 행 높이 25px 및 가로 넘침 없음 확인.

2026-09-23 DB 영속화 검증(DS-002~004): 전체 pytest 49건과 JS 문법 검사 통과. SQLite 새 연결·스키마 재적용·이름 변경/충돌·커밋 실패 복원·이미지 교체·선택 리비전·SSE 오류/락 반환을 검증했다. 기존 마이그레이션 10·11을 보존하고 새 이력은 12로 기록했다. 실제 CUDA 추론으로 인터넷등기소 1,000장, 대법원·정부24·위택스 각 500장, 총 2,500장의 신뢰도를 오류 없이 DB에 저장했고 파일명·SHA-256 전체 일치를 확인했다. 서버 코드 재로딩 뒤 API에서 모든 값을 다시 조회했고, 독립 Chromium 세션 2개에서 인터넷등기소 1,000장 표시와 계산 버튼 재실행 시 0건 처리, 색상·정렬·새로 고침을 확인했다. 937×646 및 390×844에서 가로 넘침·페이지 JS 오류가 없었다. 기존 Starlette deprecation 경고 2건 외 실패·차단 항목은 없다.

2026-09-23 수동 편집·가변 글자 크기 검증(DS-005~006): 전체 pytest 55건 및 JS 문법 검사 통과. 편집 전·후 이름과 UTC 시각 저장, 반복 편집 이력 유지, API 재접속 조회, 자동 변경/변경 없음/실패의 기록 제외, DB 커밋 실패 복원, 이미지 교체 시 배지 무효화를 확인했다. 개발 URL에서 임시 복사 이미지로 실제 수동 저장 2회와 SQLite 이력 2건, 새로고침 후 체크 유지 및 신뢰도 보존을 검증했다. 937×646·390×844에서 배지가 신뢰도 줄 왼쪽에 배치되고 넘침·페이지 JS 오류가 없었다. 표시 폭 축소 시 레이블 글자가 30.1px→12.9px로 갱신됨을 확인했다. 원본 파일은 보존했고 임시 이미지와 해당 DB 기록은 제거했다. 기존 Starlette 경고 2건 외 실패·차단은 없다.

2026-09-23 DS-006 확대 검증: 비례 계수 0.55→0.62, 상한 32→36px. JS 문법·Node 회귀와 프로젝트 venv의 pytest 2건 통과. 개발 URL 937×646에서 레이블 34.7px 및 가로 넘침 없음을 확인했다.

2026-09-23 포커스 이탈 저장 검증(DS-005): `blur`에서 비어 있지 않은 라벨을 저장하고 값이 그대로여도 확인 이력을 기록하도록 변경했다. 마지막 카드 Enter도 포커스를 해제하며 저장 중 중복 요청은 차단한다. 지정 pytest 40건과 JS 문법 검사 통과. SQLite/API 회귀로 변경 없는 확인·반복 확인 후 이름 변경 시 이력 연결·확인 실패를 검증했다. Browser 플러그인이 없어 설치된 Playwright로 개발 URL 937×646·390×844를 확인했다. 테스트용 API 응답을 사용해 클릭/Tab/마지막 Enter 저장, 빈 값 제외, 체크 표시·새로고침 복원, 오류 시 입력 복구와 가로 넘침 없음을 검증했다. 페이지 JS 오류는 없었으며 콘솔의 HTTP 400은 의도한 실패 응답이다. 실제 사용자 이미지·운영 DB의 수정은 실행하지 않았다. 기존 Starlette 경고 2건은 유지된다.

2026-09-23 정렬·파일명 검증(DS-003·005): 신뢰도 내림차순으로 변경하고 동률 생성 순서·미예측 후순위·편집 중 정렬 보류를 유지했다. 기존 포커스 이탈 저장 경로가 실제 `<레이블>.png` 파일명 변경을 수행함을 API 테스트에서 파일 존재·내용으로 검증했다. 지정 pytest 40건과 JS 문법 검사를 통과했다(기존 Starlette 경고 2건). Browser 플러그인이 없어 설치된 Playwright로 개발 URL의 실제 카드 500장 내림차순과 937×646·390×844 화면을 확인했다. 테스트용 API 응답으로 Tab 포커스 이탈 → 이름 변경 요청 → 카드 파일명·이미지 URL·체크 갱신 → 새로고침 복원을 검증했으며 가로 넘침·콘솔/페이지 오류가 없었다. 실제 파일 변경 검증은 pytest 임시 파일·DB에서 수행했고 사용자 이미지·DB는 수정하지 않았다.

2026-09-23 체크 이미지 이동 버튼 검증(DS-007): `체크된 이미지 train으로 이동` 버튼과 POST API를 추가했다. 지정 pytest 45건 및 JS 문법 검사 통과. 임시 파일·SQLite로 체크/미체크 분리, 내용·mtime·이력 보존, 재실행, 대상 리비전 분리, 충돌, 이미지 교체, unlink 실패 복원, 경로 검증 및 작업 중 409를 검증했다. Node 회귀로 저장 대기·중복 요청 차단·충돌 안내·실패 후 잠금 해제를 확인했다. Browser 플러그인이 없어 설치된 Playwright로 개발 URL 937×646·390×844에서 버튼 클릭 → 지연된 blur 저장 → 단일 이동 요청 → 완료 표시·목록 갱신을 테스트용 API 응답으로 확인했고 가로 넘침·콘솔/페이지 오류가 없었다. 버튼 검증 중 사용자 이미지·DB는 변경하지 않았다. 기존 Starlette 경고 2건이 남는다.
